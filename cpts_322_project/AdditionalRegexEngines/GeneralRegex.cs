namespace cpts_322_project {
    // Any supported flavor of regex
    public abstract class GeneralRegex {
        // returns true if the regex matches the given string
        public abstract bool MatchesString(string input);
    }
}